import { helperTextColor } from '../color';
import styled from '../../util/styled';

/**
 * Plain text used in forms.
 */
export const PlainText = styled.p`
  margin: 0.2em 0;
  color: ${helperTextColor};
`;
